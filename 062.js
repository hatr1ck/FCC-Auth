const express = require('express'); 
const app = express();

/* 
The common practice is to have port stored as a variable. Let's create a `port` varibale with the `const` keyword and set it to `3000`.
*/