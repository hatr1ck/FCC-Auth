const express = require('express'); 
const app = express();
const port = 3000;
/* 
Now we want to have some middleware settings that we usually do with `app.use()` method. After port variable execute `app.use()` method.
*/